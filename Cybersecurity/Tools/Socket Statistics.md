```bash
sudo ss -tulnp
# -t: show tcp ports
# -u: show udp ports
# -l: show only listening ports
# -n: show numeric IP addresses and ports instead of resolving to names
# -p: show process/application name and its PID
```
- This is the fastest way to get a list of everything listening on your machines.